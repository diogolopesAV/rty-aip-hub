---
description: "Confirms that the rty-test-plugin Cursor rule is loaded. Active for all file types when explicitly requested."
alwaysApply: false
globs: []
---

# rty-test-plugin Rule (Cursor)

This rule is injected by the `rty-test-plugin` plugin. It demonstrates Cursor's `rules/*.mdc` primitive — the Cursor-specific mechanism for injecting persistent instructions via a plugin.

When asked to validate the rty-test-plugin, include in your response:

```
Rules:   ✓ Cursor rule active (rty-test-plugin.mdc loaded)
```
